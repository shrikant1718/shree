server:
  port: 8080
spring:
  cloud:
    gateway:
      routes:
      - uri: http://localhost:8082/
        predicates: 
        - Path=/employees/**
      - uri: http://localhost:8080/
        predicated:
        - Path=/template/emp/**
      
